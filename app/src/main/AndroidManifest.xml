<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vendas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_android"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_android"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".produtoView"></activity>
        <activity android:name=".produtoEditar" />
        <activity android:name=".produto" />
        <activity android:name=".fabeditar" />
        <activity android:name=".fabbrandview" />
        <activity android:name=".Fabricante" />
        <activity android:name=".categoriaEditar" />
        <activity android:name=".categoriaView" />
        <activity android:name=".Categoria" />
        <activity android:name=".Main" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>